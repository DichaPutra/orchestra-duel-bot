# ── Bot Mode ──
# BOT_MODE: hybrid (RAM + Vision) atau memory_only (Pure RAM, tanpa API/Vision)
BOT_MODE=hybrid

# LLM_PROVIDER: gemini, openai, openrouter, groq, deepseek
LLM_PROVIDER=gemini

# ── Google Gemini ──
GEMINI_API_KEY=AIzaSy...
# GEMINI_MODEL=gemini-2.5-flash

# ── OpenAI ──
OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# ── OpenRouter (Akses Claude, DeepSeek-VL, Gemini, dll) ──
OPENROUTER_API_KEY=sk-or-...
# OPENROUTER_MODEL=google/gemini-2.5-flash

# ── Groq (Llama 3.2 Vision) ──
GROQ_API_KEY=gsk_...
# GROQ_MODEL=llama-3.2-11b-vision-preview

# ── DeepSeek Official (Text-only decision) ──
DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_MODEL=deepseek-chat
