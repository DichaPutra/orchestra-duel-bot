# Core
Pillow>=10.0.0

# Screenshot (Windows)
dxcam; sys_platform == 'win32'
# Screenshot (fallback cross-platform)
mss>=9.0.0

# OCR lokal (optional — bisa ganti pake LLM Vision)
easyocr>=1.7.0

# LLM API
requests>=2.31.0
python-dotenv>=1.0.0

# Input simulation
pyautogui>=0.9.54
# Window detection (Windows)
pygetwindow>=0.0.9; sys_platform == 'win32'
# Window detection (macOS)
pyobjc-framework-Quartz; sys_platform == 'darwin'

# Utilitas
numpy>=1.24.0
